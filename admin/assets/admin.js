/**
 * Handsome Pete Booking — Admin JS
 * Loaded on all hp-booking admin pages.
 * Page-specific logic lives in inline <script> blocks.
 */
(function($) {
    'use strict';
    // Intentionally minimal — page scripts use phpBookingAdmin.ajaxUrl / .nonce
})(jQuery);
